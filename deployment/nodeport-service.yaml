apiVersion: v1
kind: Service
metadata:
  name: oauth-auth-service-nodeport-service
  namespace: oauth-auth-service-namespace
spec:
  type: NodePort
  selector:
    app: oauth-auth-service-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30001
